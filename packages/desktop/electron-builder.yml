npmRebuild: false
appId: ai.codius.desktop
productName: Codius
executableName: Codius
protocols:
  - name: Codius agent link
    schemes:
      - codius
afterPack: ./scripts/after-pack.js
afterSign: ./scripts/after-sign.js
directories:
  output: release
files:
  - dist/**/*
  - "!**/*.map"
  - "!node_modules/@codius.ai/*/src/**"
  - "!node_modules/@codius.ai/**/*.test.*"
  - "!node_modules/@codius.ai/**/*.spec.*"
  - "!node_modules/@codius.ai/server/dist/server/web-ui/**"
asarUnpack:
  - dist/daemon/node-entrypoint-runner.js
  - node_modules/@codius.ai/server/dist/server/terminal/shell-integration/**/*
extraResources:
  - from: ../app/dist
    to: app-dist
  - from: ../../skills
    to: skills
  - from: assets/editor-targets
    to: editor-target-icons
publish:
  provider: github
  owner: prismosoft
  repo: codius-app
mac:
  artifactName: "Codius-${version}-${arch}.${ext}"
  category: public.app-category.developer-tools
  icon: assets/codius-icon.png
  hardenedRuntime: true
  notarize: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.inherit.plist
  extraResources:
    - from: bin/codius
      to: bin/codius
  target:
    - dmg
    - zip
linux:
  category: Development
  icon: assets/codius-icon.png
  artifactName: "Codius-${version}-${arch}.${ext}"
  maintainer: "Prismosoft <prismosoft@gmail.com>"
  vendor: "Codius"
  extraResources:
    - from: bin/codius
      to: bin/codius
  target:
    - AppImage
    - deb
    - rpm
    - tar.gz
appImage:
  # Keep the AppImage filename stable across versions (no ${version}) so
  # electron-updater overwrites the file in place instead of renaming it to a new
  # versioned path. A rename would orphan the old binary and break shortcuts.
  artifactName: "Codius-${arch}.${ext}"
rpm:
  packageName: codius-app
win:
  artifactName: "Codius-Setup-${version}-${arch}.${ext}"
  icon: assets/codius-icon.png
  extraResources:
    - from: bin/codius.cmd
      to: bin/codius.cmd
  target:
    - target: nsis
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
