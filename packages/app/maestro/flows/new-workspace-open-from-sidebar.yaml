# NOTE: Render through the workspace-create shell scripts before running when
# using ${CODIUS_MAESTRO_*}. Maestro does not substitute environment vars.
appId: ${CODIUS_MAESTRO_APP_ID}
---
# Start from an open Android sidebar with ${CODIUS_MAESTRO_PROJECT_NAME}
# visible. Ends on the New Workspace screen.

- assertVisible:
    id: "sidebar-project-list"

- assertVisible: ${CODIUS_MAESTRO_PROJECT_NAME}

- tapOn: "Create a new workspace for ${CODIUS_MAESTRO_PROJECT_NAME}"

- extendedWaitUntil:
    visible: "New workspace"
    timeout: 30000
